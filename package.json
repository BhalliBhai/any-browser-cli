{
  "name": "any-browser-cli",
  "version": "1.0.0",
  "description": "Test your website in different browsers like:  Safari | Chrome | Firefox | Opera etc",
  "main": "index.js",
  "scripts": {
    "test:chrome": "npx playwright test --headed --browser=chromium",
    "test:firefox": "npx playwright test --headed --browser=firefox",
    "test:safari": "npx playwright test --headed --browser=webkit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
